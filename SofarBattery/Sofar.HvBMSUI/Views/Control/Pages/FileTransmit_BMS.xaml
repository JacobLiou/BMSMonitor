<UserControl x:Class="Sofar.HvBMSUI.Control.Pages.FileTransmit_BMS"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sofar.HvBMSUI.Control.Pages" 
             xmlns:viewmodels="clr-namespace:Sofar.HvBMSUI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:FileTransmit_BMS_ViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1280">


    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid>
            <!-- 2行 1 列 GroupBox 布局 -->
            <Grid Grid.Column="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <GroupBox   Grid.Row="0" Grid.Column="0" Header="文件传输" FontSize="14" Margin="10,0,0,10"  Style="{x:Null}">
                    <StackPanel Margin="20" >
                        <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                            <TextBlock  Margin="0,0,0,0" Text="BCU设备地址: "   VerticalAlignment="Center" TextAlignment="Center" />
                            <TextBox    Margin="8,0,0,0" Height="30" Width="170"  Text="{Binding slaveAddress, Mode=TwoWay}" />
                            <TextBlock  Text="BMU设备地址: "   Margin="25,0,0,0"  VerticalAlignment="Center" TextAlignment="Center" />
                            <TextBox    Margin="8,0,0,0" Height="30" Width="170"  Text="{Binding subDeviceAddress, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="5,10,0,0">
                            <TextBlock Grid.Column="1" Grid.Row="2" Width="100" VerticalAlignment="Center"  Text="文件编号: " />
                            <ComboBox x:Name="ckbFileNumber" Width="170" Margin="0,10,0,0" BorderThickness="1" ItemsSource="{Binding FileNumberList}"   SelectedItem="{Binding SelectedFileNumber,Mode=TwoWay}"/>
                            <CheckBox x:Name="ckReadAll" Content="是否读取全部文件" IsChecked="{Binding IsReadAll_Checked}" Margin="20,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"  />
                            <TextBlock  Text="读取文件条数: "  Margin="50,0,0,0"  VerticalAlignment="Center" TextAlignment="Center" Visibility="{Binding VisibleAttributes}"/>
                            <TextBox    Margin="5,0,0,0" Height="30" Width="50"  Text="{Binding readCount, Mode=TwoWay}"  Visibility="{Binding VisibleAttributes}"/>
                            <TextBlock  Margin="20,0,0,0" Text="文件起始位置: "   VerticalAlignment="Center"   TextAlignment="Center" Visibility="{Binding VisibleAttributes}"/>
                            <TextBox    Margin="5,0,0,0" Height="30" Width="50"  Text="{Binding StartLocal, Mode=TwoWay}" Visibility="{Binding VisibleAttributes}"  />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="5,10,0,0">
                            <TextBlock Grid.Column="1" Grid.Row="2" Width="100" VerticalAlignment="Center"  Text="模块名称: " />
                            <ComboBox x:Name="ckbModeName" Width="170" Margin="0,10,0,0" BorderThickness="1" IsEnabled="{Binding IsModeNameEnable}" ItemsSource="{Binding ModeNameList}"  SelectedItem="{Binding SelectedModeName,Mode=TwoWay}"/>
                            <Button  Width="120" Margin="405,0,0,0" Content="{Binding FileTransmitStatus, Mode=TwoWay}" IsEnabled="{Binding IsTransmitEnable}"  VerticalAlignment="Center" Command="{Binding FileTransmitCmd}" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>


                <ListView Name="lvUpgradeLog" Grid.Row="1" Grid.Column="0" Margin="10,30,0,0" ItemsSource="{Binding FileTransmitLogList}">
                    <ListView.Resources>
                        <!-- 自定义样式 -->
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="2"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFD3E8FF"/>
                                    <!-- 鼠标悬浮背景色 -->
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.Resources>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="时间" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LogTime}" TextAlignment="Center"  VerticalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="帧ID" Width="200" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FrameID}" TextAlignment="Center"  VerticalAlignment="Center"  />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="日志数据" Width="400">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LogData}" TextWrapping="Wrap" TextAlignment="Center"  VerticalAlignment="Center"  />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>



            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
